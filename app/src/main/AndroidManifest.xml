<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lww.shipei">

    <application
        android:name=".density.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".shipeiliuhai.DisplayCutouActivity"></activity>
        <activity android:name=".density.BaseDesityActivity" />
        <activity android:name=".density.DestityActivity" />
        <activity android:name=".baifenbi.MyPercentActivity" />
        <activity android:name=".baifenbi.BaiFenBiMainActivity" />
        <activity
            android:name=".PX_SHIPEI.PxActivity"
            android:configChanges="orientation|keyboard|layoutDirection|screenSize" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>